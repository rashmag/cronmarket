<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/backOnboard"
    tools:background="@drawable/back_onboard_one"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/imgOnboard"
        android:layout_width="328dp"
        android:layout_height="237dp"
        android:layout_marginTop="152dp"
        tools:src="@drawable/delivery_onboard"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/titleOnboard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dimen_32"
        android:fontFamily="@font/sf_pro_text_regular"
        android:text="@string/pay_title_onboard"
        android:textColor="@color/white"
        android:textFontWeight="700"
        android:textSize="20sp"
        android:textAlignment="center"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgOnboard"
        tools:targetApi="p" />

    <TextView
        android:id="@+id/subTitleOnboard"
        android:layout_width="wrap_content"
        android:layout_marginHorizontal="@dimen/dimen_20"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dimen_6"
        android:text="@string/sub_title_pay_onboard"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textFontWeight="500"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleOnboard"
        tools:targetApi="p" />
</androidx.constraintlayout.widget.ConstraintLayout>